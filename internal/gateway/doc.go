// Package gateway implements the MCP server side of symbrain: the handshake
// with the harness, catalog merging, tool namespacing, request routing to
// the broker, and error mapping.
package gateway
