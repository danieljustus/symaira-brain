// Package profile defines the profile schema, loading, validation, and
// defaults for symbrain. A profile represents one harness connection and
// controls which state cores (vault, memory, skills) are exposed and how.
package profile
