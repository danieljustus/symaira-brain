<!-- coverage: timestamp=2026-08-05T15-13-27Z repo=danieljustus/symaira-brain head=e07e0ecb9cbc20ef7b7a39325ebbfea0c8d5bb53 overall=83.7 patch=100.0 baseline=80.8 delta=+2.9 mutation=n/a assertdensity=3.26 slowtests=3 suite=24.6 -->

# Coverage Review - 2026-08-05

## Summary
- Tooling: Go 1.26.5, `go test -coverprofile` + `go tool cover` (same method as `make coverage` / the CI coverage job)
- Command: `make coverage`
- Result: pass (all packages green; no coverage gate is enforced in CI)
- Overall: lines 83.7% (statements)
- Patch coverage: 100.0% of changed executable Go lines; the changed `syncTarget` path construction is exercised by sync tests
- Baseline: 80.8% (2026-07-24 report, head 83e89f9 — provenance-verified ancestor) → delta **+2.9**
- Mutation score: n/a (the configured go-mutesting workflow documents incompatibility with Go 1.26 and publishes no fabricated score)
- Test quality: assert density 3.26/test over 334 test functions / 1089 assertion tokens; the five zero-assert matches are `TestMain` hooks, a shared-helper-backed large-diff test, and deliberate nil-receiver smoke tests; AAA no systemic issues; suite 24.6s wall, 3 tests over 1s (real subprocess/integration tests)
- Gate: threshold=80 (default, no repo gate found) → overall 83.7% satisfies the default bar

## Metrics
| Area | Lines | Notes |
|---|---:|---|
| `cmd/symbrain` | 79.5% | command entry-point coverage remains the main low package, but the prior CLI wiring gap is covered |
| `internal/audit` | 88.2% | large-file tail and degraded logging paths covered |
| `internal/gateway` | 92.2% | ServeIO startup/error paths covered |
| `internal/harness` | 78.2% | remaining uncovered branches are mostly serializer/platform edge cases |
| `internal/output` | 97.6% | format validation, conflict handling, rendering errors and table/JSON paths covered |
| `internal/skills` | 95.7% | timeout and runner paths covered |
| `internal/sync` | 90.3% | summary message/skills branches and path construction covered |
| `internal/xdg` | 86.7% | resolver tests cover env and fallback paths |
| **Total** | **83.7%** | **up from 80.8% baseline (+2.9)** |
| **Patch (changed lines)** | **100.0%** | **2 of 2 changed executable lines covered** |

## Test quality
- **Mutation:** n/a — no trustworthy Go mutation score is available under Go 1.26.
- **Assert density:** 3.26 asserts/test; no systemic assertion-free test pattern. Zero-assert detections are deliberate hooks/smoke coverage or tests whose assertions live in a shared helper.
- **AAA:** no systemic issues; tests use focused table-driven or arrange/act/assert structures.
- **Performance:** suite 24.6s; slowest tests were `TestCmdServe_StdioHandshake` 13.86s, `TestStdioHygiene_ServeEmitsOnlyJSONRPC` 1.26s, and `TestSync_SymskillsAvailable` 1.16s. These exercise real subprocess/stdio boundaries and are appropriately separated.

## Findings

No new issue-worthy coverage findings. The prior baseline findings for CLI command wiring, gateway startup/error paths, audit tail/degraded behavior, and XDG resolution are covered on the current tree. The remaining `internal/harness` gap is concentrated in low-frequency serializer and platform branches and does not block the 80% default bar.

## Simplification

- Removed the unused `toOrderedMap` helper from `internal/harness/ordered_map.go`.
- Replaced manual slash-based path formatting in `internal/sync/sync.go` with `filepath.Join`, reducing string-formatting logic and preserving platform-correct paths.

## Verification

- `make test`
- `make lint` (golangci-lint unavailable; fallback `go vet ./...` passed)
- `make build`
- `make fmt-check`
- `make coverage`
