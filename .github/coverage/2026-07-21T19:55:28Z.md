<!-- coverage: timestamp=2026-07-21T19:55:28Z repo=danieljustus/symaira-brain head=8f87332455242002781870dd90030851c5d6f532 overall=72.3 patch=n/a baseline=72.3 delta=+0.0 mutation=n/a assertdensity=3.3 slowtests=0 suite=12 -->

# Coverage Review - 2026-07-21

## Summary
- Tooling: `go test` + `go tool cover`
- Command: `go test ./... -coverprofile=coverage.out && go tool cover -func=coverage.out`
- Result: pass
- Overall: lines 72.3% (statements)
- Patch coverage: n/a — the unreleased diff `v0.1.0..HEAD` contains zero Go changes (native Swift apps + docs only)
- Baseline: 72.3% → 72.3% (delta +0.0); baseline measured at `v0.1.0` per `--baseline-ref`; identical because no Go file changed
- Mutation score: n/a (no mutation tool configured for Go in this repo)
- Test quality: assert density 3.3/test (736 assertions over 225 test funcs), no zero-assert test files; AAA no systemic issues (table-driven tests throughout); suite ~12s cold, 0 slow tests
- Gate: no repo-configured threshold; 80 default (no repo gate found) → overall 72.3% below default bar = WARN, not FAIL

## Metrics
| Area | Lines | Notes |
|---|---:|---|
| `internal/skills` | 75.7% | |
| `internal/sync` | 79.4% | |
| `internal/xdg` | 0.0% | trivial path resolution, no test files |
| **Total** | 72.3% | |
| **Patch (changed lines)** | n/a | no Go diff vs `v0.1.0` |

## Test quality
- **Mutation:** n/a — no Go mutation tool (gremlins) configured; not run.
- **Assert density:** 3.3 asserts/test overall; zero-assert tests: none detected (every `*_test.go` contains `t.Error/t.Fatal/require./assert.` tokens).
- **AAA:** no systemic issues — table-driven tests with subtests across all packages.
- **Performance:** full suite ~12s cold (cached <1s); no test over the 1s advisory threshold except integration-style broker/sync tests within normal range.

## Findings
- [ ] **[Coverage/WARN] Overall coverage 72.3% below the default 80% bar**
  - **Status quo:** `go tool cover` total 72.3% statements; lowest packages: `internal/xdg` 0.0% (no test files), `internal/skills` 75.7%, `internal/sync` 79.4%.
  - **Likely cause:** No repo-enforced coverage gate; growth tracked feature velocity.
  - **Proposed solution:** Add unit tests for `internal/xdg` path resolution (env overrides) and error paths in `internal/skills`/`internal/sync` orchestration.
  - **Uncovered:** `internal/xdg/xdg.go` (whole file, in `ConfigDir/ProfilesDir/DataDir/AuditDir/CacheDir`); `internal/skills/skills.go` error branches (in `Render`); `internal/sync/sync.go` error branches (in `Run`)
  - **Effort/Impact:** Low effort / medium impact

## Non-issues
- `Sources/`, `Tests/SymBrainCoreTests`, `SymBrain.xcodeproj` are Swift/Xcode artifacts; Swift decoding tests exist and pass via xcodebuild, outside Go coverage scope.

## Next step
Run `/02-gh-plan --from-coverage .github/coverage/2026-07-21T19:55:28Z.md` to create GitHub issues.
