{
  "schema_version": 1,
  "protocol": 1,
  "oracle": "go-azuretls-v0.8.0",
  "cases": [
    {"id":"compat-handshake", "protocol":1, "expected":"handshake_ack"},
    {"id":"compat-pinned-identity", "component":"symbrowse-go", "oracle":"go-azuretls-v0.8.0", "fallback":"forbidden"},
    {"id":"compat-six-profiles", "profiles":["chrome","edge","firefox","ios","opera","safari"], "fixture":"loopback-http"},
    {"id":"compat-request-id", "request_ids":[1,2,3,4,5,6,7], "expected":"monotonic across clean sidecar restart"},
    {"id":"compat-bounded-frame", "max_frame_bytes":1048576, "expected":"typed frame_too_large before unbounded allocation"},
    {"id":"compat-integrity-error", "expected_errors":["compat_protocol_mismatch","compat_identity_mismatch"], "fallback":"forbidden"},
    {"id":"compat-typed-fetch-error", "expected_error":"compat_fetch_failed", "retryable":false},
    {"id":"compat-timeout-restart", "expected":"late response is discarded and next request succeeds"},
    {"id":"compat-clean-exit", "expected":"stdin EOF exits sidecar and client can restart it"},
    {"id":"compat-private-endpoint", "directory_mode":"0700", "endpoint_mode":"0600", "stdio":"child-private pipes"},
    {"id":"compat-rollback-go", "build":"CGO_ENABLED=0 go build -trimpath -o target/compat-sidecar/symbrowse-compat ./cmd/symbrowse", "entrypoint":"compat-sidecar"}
  ]
}
